{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake build and run",
            "type": "shell",
            "command": "cmake .. -G 'Visual Studio 17 2022'; cmake --build ./; cd ./Debug; ./DISSH.exe",
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}